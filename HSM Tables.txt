			PostgreSQL
//CRUD
create table registered_user(
	userid serial primary key,
	username varchar(20) unique not null,
	password varchar(30) not null,
	email varchar(30),
	role varchar(20), --doctor/patient/admin
       
);

//CRUD
create table doctor(
	doctor_id numeric(4) primary key,
	doctor_name varchar(30) not null,
	specialization varchar(50),
	available_from time,
	available_to time,
	hospital_attached_to varchar(30),
	qualification varchar(100),
	experience_in_years numeric(2),
	mobile numeric(10),
	email varchar(30),
	username varchar(20) unique not null        
);


//CRUD
create table patient(
	patient_id numeric(4) primary key,
	patient_name varchar(30) not null,
	gender varchar(10),
	birthdate date,
	blood_group varchar(5),
	mobile numeric(10),
	address varchar(50),
	email varchar(30),
	username varchar(20) unique not null	
);

//CR
create table doctor_patient(
 	doctor_patient_id numeric(4) primary key,
        doctor_id numeric(4),
	patient_id numeric(4),
	appointment_date date,	
	constraint fk_doctorid foreign key(doctor_id) references doctor(doctor_id),
	constraint fk_patientid foreign Key(patient_id) references patient(patient_id)

);

//CRUD
create table patient_case_sheet(
	patient_case_sheet_id numeric(4) primary key,
	patient_name varchar(30),
	case_sheet_open_date date,
	case_sheet_close_date date,
	status char(10),
	hostipal_details varchar(100),
	health_statistics varchar(100),
	symptoms varchar(100),
	prescription varchar(100),
	diet varchar(100),	
	patient_id numeric(4),
	doctor_id numeric(4)),        
	constraint fk_doctid foreign key(doctor_id) references doctor(doctor_id),
        constraint fk_patientid foreign key(patient_id) references patient(patient_id)
        
);

	Postgres

create table patient_history(
	patient_history_id numeric(4) primary key,
	patient_name varchar(30),
	case_sheet_open_date date,
	case_sheet_close_date date,
	hostipal_details varchar(100),
	symptoms varchar(100),
	prescription varchar(100),
	diet varchar(100),
	hostipal_details varchar(100)
	patient_id numeric(4),	
        constraint fk_patient_id foreign key(patient_id) references patient(patient_id)
);

	MongoDB

	Collection Name: patient_history

	document:
	{
	  "patient_history_id": 
	   patient_name:
	   case_sheet_open_date :,
	   case_sheet_close_date :,
	   hostipal_details :,
	   health_statistics:,
	   symptoms: ,
	   prescription :,
	   diet :,	 
	   patient_id :
	}






